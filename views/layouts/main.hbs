{{> head}}
<body>
{{> navbar }}

    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                {{#if success_msg}}
                    <div class="materialert success">
                        {{success_msg}}
                    </div>
                {{/if}}

                {{#if error_msg}}
                    <div class="materialert error">
                        {{error_msg}}
                    </div>

                {{/if}}

                {{#if error}}
                    <div class="materialert error">
                        {{error}}
                    </div>

                {{/if}}
            </div>
        </div>

        {{{body}}}
    </div>
</body>
</html>